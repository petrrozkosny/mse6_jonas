lokalita max srazek:=VAR _tabulka =
ADDCOLUMNS(ALL(data[lokalita]);"mesicni srazky";CALCULATE(SUM(data[srazky])))

VAR _max_srazky = MAXX(_tabulka;[mesicni srazky])

RETURN CONCATENATEX(FILTER(_tabulka;[mesicni srazky] = _max_srazky);data[lokalita];", ")
